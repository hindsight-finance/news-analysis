---
phase: 01-primary-pipeline-on-polars
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [main.py]
autonomous: true
requirements: [MIGRATE-01, ENV-02]

must_haves:
  truths:
    - "Running python3 main.py produces data/sweep_analysis_results.parquet on polars with zero pandas imports"
    - "polars reads nq_1m.parquet and economic_events.parquet natively (use_pyarrow=False), no pandas in the read path (ENV-02)"
    - "The output parquet has the exact 21-column CONTRACT_SCHEMA (names + dtypes pinned)"
    - "Events are NOT silently dropped: main.py reports N>0 analyzed events (us/ns reconciled)"
    - "The numpy sweep methodology kernel is preserved verbatim (only the I/O boundary changed)"
  artifacts:
    - path: "main.py"
      provides: "Polars sweep engine + module-level CONTRACT_SCHEMA + threaded lookups dict (no nq.attrs)"
      contains: "CONTRACT_SCHEMA"
    - path: "data/sweep_analysis_results.parquet"
      provides: "Regenerated 21-col data contract consumed by exploration.py + causal_analysis.py"
  key_links:
    - from: "main.py load_data"
      to: "data/nq_1m.parquet + data/economic_events.parquet"
      via: "pl.read_parquet(use_pyarrow=False)"
      pattern: "read_parquet"
    - from: "main.py load_data"
      to: "analyze_event"
      via: "lookups dict (utc_values/et_values ns int64 arrays) threaded explicitly, replacing nq.attrs"
      pattern: "lookups"
    - from: "main.py main()"
      to: "data/sweep_analysis_results.parquet"
      via: "pl.DataFrame(results, schema=CONTRACT_SCHEMA).write_parquet"
      pattern: "schema=CONTRACT_SCHEMA"
---

<objective>
Port `main.py` (the sweep engine) from pandas to polars. Touch ONLY the I/O boundary — load, candle lookup, column→numpy extraction, output write — and preserve the numpy methodology kernel (sweep direction, first-target classification, synthetic box, MAE, argmax timing) VERBATIM. Pin the intermediate `data/sweep_analysis_results.parquet` to the exact 21-column CONTRACT_SCHEMA so the data contract consumed by the two downstream scripts cannot drift. Resolve ENV-02: polars reads both raw inputs natively with no pandas in the read path.

Purpose: `main.py` is the producer of the data contract and the only Phase-1 script that touches the irreplaceable raw inputs. Protecting the methodology kernel and the contract schema protects the research asset.
Output: A polars `main.py` that regenerates the 21-col contract with N>0 rows and zero pandas imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md
@.planning/phases/01-primary-pipeline-on-polars/01-VALIDATION.md
@main.py
@smoke/phase1_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the main.py data + lookup layer to polars (load_data, lookups threading, candle helpers)</name>
  <files>main.py</files>
  <read_first>
    - main.py (the current pandas implementation — read fully so the real I/O boundary, not assumptions, is ported; especially ensure_utc, timestamp_ns_utc, find_sorted_pos, add_lookup_tables, load_data, get_release_candle, get_candles_until_eod, get_candle_at_time)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md (Component Responsibilities port-impact table main.py:24-133; Pitfall 1 us/ns; Pitfall 3 no .attrs; Pitfall 7 DST anchors; Code Examples: "Derive naive ET from UTC", "Build the two ns lookup arrays", "Iterate events")
    - smoke/phase1_smoke.py (the --check read and --check contract signals this layer must keep green)
  </read_first>
  <action>
    Replace `import pandas as pd` with `import polars as pl`. Add `from datetime import datetime, time, timedelta, timezone`; keep `from zoneinfo import ZoneInfo`, `import numpy as np`, `from pathlib import Path`. Keep the `DATA_DIR` / `OUTPUT_FILE` / `TRADING_DAY_END` constants.

    Port `ensure_utc`: instead of a Series helper, normalize the timezone inside `load_data` using polars exprs — `pl.col(c).dt.replace_time_zone("UTC")` when the column is tz-naive, `pl.col(c).dt.convert_time_zone("UTC")` when tz-aware (events `datetime_utc` is already ns/UTC; nq `datetime_utc` is us/UTC).

    Port `timestamp_ns_utc` to a stdlib scalar helper that converts a python `datetime` (tz-aware, or naive treated as UTC) to an integer of UTC nanoseconds WITHOUT float rounding: localize to UTC if naive (`replace(tzinfo=timezone.utc)`), then compute `int(dt.timestamp()) * 1_000_000_000 + dt.microsecond * 1000`. (Events are minute-aligned so this is exact.)

    Keep `find_sorted_pos` BYTE-FOR-BYTE UNCHANGED (it is pure numpy and methodology-adjacent).

    Delete `add_lookup_tables` and the `nq.attrs` mechanism entirely (Pitfall 3: polars has no `.attrs`). Instead build the two ns-int64 lookup arrays inside `load_data` and thread them explicitly: `utc_values = nq.get_column("DateTime_UTC").to_numpy().astype("datetime64[ns]").astype("int64")` and `et_values = nq.get_column("DateTime_ET").to_numpy().astype("datetime64[ns]").astype("int64")` — the `.astype("datetime64[ns]")` step FORCES microseconds→nanoseconds (Pitfall 1; without it every event is silently dropped 1000x off).

    Port `load_data` to return a 3-tuple `(events, nq, lookups)` where `lookups = {"utc_values": ..., "et_values": ...}`: read both inputs with `pl.read_parquet(..., use_pyarrow=False)` (ENV-02 native read — do not route through pandas); rename nq `datetime_utc`→`DateTime_UTC` if needed; normalize `DateTime_UTC` to UTC; derive `DateTime_ET` via `pl.col("DateTime_UTC").dt.convert_time_zone("America/New_York").dt.replace_time_zone(None)`; normalize events `datetime_utc` to UTC; `nq = nq.sort("DateTime_UTC")`; build the two ns arrays; return `(events, nq, lookups)`.

    Port the three candle helpers to accept `lookups` explicitly (signatures change — this is the required mechanical no-`.attrs` change): `get_release_candle(nq, lookups, event_time)` → `find_sorted_pos(lookups["utc_values"], timestamp_ns_utc(event_time))` then `nq.row(pos, named=True)` (a dict) or `None`; `get_candle_at_time(nq, lookups, target_et)` → `find_sorted_pos(lookups["et_values"], <target_et as ns int>)` then `nq.row(pos, named=True)` or `None`; `get_candles_until_eod(nq, lookups, start_time)` → searchsorted start, build the 16:00-ET end-of-day anchor with stdlib `datetime` from the naive ET row value (next-day +1 when `start_et.time() >= TRADING_DAY_END`), localize ET→UTC via `ZoneInfo("America/New_York")` then `.astimezone(timezone.utc)` (Pitfall 7: all anchors are DST-safe), convert to ns int, `np.searchsorted(lookups["utc_values"], end_ns, side="right")` for `end_pos`, and return `nq.slice(start_idx + 1, end_pos - (start_idx + 1))` (a polars DataFrame; use `.is_empty()` downstream). Preserve the "None / empty for not-found" idiom exactly.

    Do NOT yet rewrite the bodies of `analyze_event`, `get_session_context`, or `main()` beyond updating their calls to the new `lookups` signatures enough to keep the module importable — Task 2 ports those bodies.
  </action>
  <verify>
    <automated>python3 -c "import main; e,nq,lk=main.load_data(); import numpy as np; assert isinstance(lk,dict) and lk['utc_values'].dtype==np.int64 and lk['et_values'].dtype==np.int64; assert int(lk['utc_values'][0]) > 10**18; assert 'DateTime_ET' in nq.columns; print('load OK rows', nq.height)" && ! grep -nE "nq\.attrs|^[[:space:]]*import pandas|^[[:space:]]*from pandas" main.py && python3 smoke/phase1_smoke.py --check read</automated>
  </verify>
  <acceptance_criteria>
    - `main.load_data()` returns a 3-tuple `(events, nq, lookups)`; `lookups` is a dict with `utc_values` and `et_values` numpy int64 arrays.
    - The lookup arrays are NANOSECOND scale: `int(lookups["utc_values"][0]) > 10**18` (catches the us/ns Pitfall-1 mistake before it silently drops events).
    - `nq` is a polars DataFrame with both `DateTime_UTC` (UTC-aware) and `DateTime_ET` (naive ET) columns; `nq` is sorted by `DateTime_UTC`.
    - `! grep -n "attrs" main.py` returns nothing (no `nq.attrs` remains) and `find_sorted_pos` body is unchanged from the original.
    - `python3 smoke/phase1_smoke.py --check read` exits 0 (native `use_pyarrow=False` read path); `! grep -nE "^[[:space:]]*(import pandas|from pandas)" main.py` returns nothing.
  </acceptance_criteria>
  <done>The data + candle-lookup boundary is ported to polars, lookups are threaded explicitly (no `.attrs`), timestamps are forced to ns, and the native read path is green.</done>
</task>

<task type="auto">
  <name>Task 2: Port analyze_event (kernel verbatim) + get_session_context + main() write with pinned CONTRACT_SCHEMA</name>
  <files>main.py</files>
  <read_first>
    - main.py (current `analyze_event` lines ~193-315 incl. the numpy kernel ~215-287, `get_session_context` ~136-190, and `main()` ~318-393 — read fully to preserve the kernel verbatim)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md (Pattern 1 "preserve the numpy kernel, port only the boundary"; Pattern 2 the exact 21-col CONTRACT_SCHEMA with `event_datetime`=Datetime('ns','UTC'), `pre_candle_volume`=Float64, `release_volume`=Int64; Pitfall 1 `times` array ns; Pitfall 2 dtype-inference drift; Assumptions A1)
    - smoke/phase1_smoke.py (the `--check contract` assertion this write step must satisfy)
  </read_first>
  <action>
    Port `analyze_event` to signature `analyze_event(nq, lookups, event_time, event_type)`. Replace pandas row/Series access with the dicts returned by the Task-1 helpers: extract `release_candle = get_release_candle(nq, lookups, event_time)`; read `data_high`/`data_low`/`Open`/`Volume` etc. by dict key. Build `subsequent = get_candles_until_eod(nq, lookups, event_time)`; guard with `if subsequent.is_empty(): return None` (replaces `.empty`). Extract `highs = subsequent.get_column("High").to_numpy()`, `lows = subsequent.get_column("Low").to_numpy()`, and `times = subsequent.get_column("DateTime_UTC").to_numpy().astype("datetime64[ns]")` — the `.astype("datetime64[ns]")` FORCES us→ns so the timing deltas are correct (Pitfall 1).

    KEEP the methodology kernel (the block computing `high_hits`/`low_hits`, first sweep direction, `first_sweep_pos`, synthetic box level, first-target classification, `box_hits`/`opposite_hits`, `mae_before_reversal`, and the `np.argmax` timing — current lines ~215-287) BYTE-FOR-BYTE UNCHANGED. The ONLY edits permitted in that region are removing the pandas scalar wrappers: replace each `pd.Timestamp(times[...])` with pandas-free timing math — compute `time_to_first_sweep` and `time_to_opposite_sweep` in minutes from the `datetime64[ns]` values using numpy deltas (e.g. `(times[a] - np.datetime64(event_time, "ns")) / np.timedelta64(1, "m")` for the first sweep, and `(times[max(...)] - times[first_sweep_pos]) / np.timedelta64(1, "m")` for the opposite). `event_time` is a python datetime from `iter_rows`; wrap it as `np.datetime64(event_time, "ns")`. Keep the produced `result` dict keys and order identical (base dict then `result.update(session_context)`).

    Port `get_session_context(nq, lookups, release_candle)`: read `DateTime_ET`/`Open`/`Volume` from the `release_candle` dict; build the 8:29 / 00:00 / 18:00 / 16:59 ET scalar anchors with stdlib `datetime`/`timedelta` from `release_et.date()` (Pitfall 7: all anchors are DST-safe); look each up via `get_candle_at_time(nq, lookups, <anchor>)` (returns a dict or None); keep the exact same computed keys (`pre_candle_range_pct`, `pre_candle_volume`, `dist_from_midnight_open_pct`, `dist_from_6pm_open_pct`, `gap_6pm_pct`, `gap_6pm_direction`, `release_volume`) and the same None defaults.

    Define a module-level `CONTRACT_SCHEMA` dict with EXACTLY the 21 columns + dtypes from RESEARCH Pattern 2, in order: event_type=pl.String, event_datetime=pl.Datetime("ns","UTC"), release_time=pl.String, data_high=pl.Float64, data_low=pl.Float64, range=pl.Float64, range_pct=pl.Float64, first_sweep=pl.String, time_to_first_sweep=pl.Float64, opposite_swept=pl.Boolean, time_to_opposite_sweep=pl.Float64, synthetic_box_breached=pl.Boolean, first_target_hit=pl.String, mae_before_reversal=pl.Float64, pre_candle_range_pct=pl.Float64, pre_candle_volume=pl.Float64, dist_from_midnight_open_pct=pl.Float64, dist_from_6pm_open_pct=pl.Float64, gap_6pm_pct=pl.Float64, gap_6pm_direction=pl.String, release_volume=pl.Int64.

    Port `main()`: iterate `for event in events.iter_rows(named=True):` calling `analyze_event(nq, lookups, event["datetime_utc"], event["title"])`; collect non-None results; build `df = pl.DataFrame(results, schema=CONTRACT_SCHEMA)` (the explicit `schema=` pins the contract and prevents Int64/us drift, Pitfall 2); write with `df.write_parquet(OUTPUT_FILE)` (no `index=` kwarg). Port the summary-print blocks (`df.groupby(...).agg(...)` → polars `df.group_by(...).agg(...)` with explicit `.sort(...)`, display-only — exact aggregate values are not contract-critical). Keep the `Successfully analyzed {len(results)} events` print. Remove every remaining pandas reference.
  </action>
  <verify>
    <automated>python3 main.py && python3 smoke/phase1_smoke.py --check contract && ! grep -nE "^[[:space:]]*(import pandas|from pandas)|pd\.|nq\.attrs" main.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 main.py` runs to completion and prints `Successfully analyzed N events` with N > 0 (a printed 0 means the us/ns reconciliation failed — Pitfall 1 — and is a FAIL).
    - `python3 smoke/phase1_smoke.py --check contract` exits 0: `data/sweep_analysis_results.parquet` has exactly 21 columns in the pinned order with `event_datetime`=`Datetime(time_unit='ns', time_zone='UTC')`, `pre_candle_volume`=`Float64`, `release_volume`=`Int64`, and height > 0.
    - `CONTRACT_SCHEMA` is a module-level dict in `main.py` with exactly 21 entries.
    - `! grep -nE "^[[:space:]]*(import pandas|from pandas)" main.py` returns nothing; `! grep -n "nq\.attrs" main.py` returns nothing.
    - Manual (per VALIDATION Manual-Only): spot-inspect a handful of output rows for sane `first_sweep` / `first_target_hit` / `mae_before_reversal` values, and confirm the numpy kernel block (sweep/box/MAE/argmax) is unchanged from the original aside from the scalar-wrapper edits.
  </acceptance_criteria>
  <done>`main.py` runs end-to-end on polars, regenerates the pinned 21-col contract with N>0 rows, preserves the methodology kernel verbatim, and contains zero pandas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.py → raw parquet inputs | `nq_1m.parquet` (us) / `economic_events.parquet` (ns) are irreplaceable, read-only |
| main.py → contract artifact | `sweep_analysis_results.parquet` is the regenerated contract consumed downstream |
| us vs ns timestamp units | mixing units in `searchsorted` silently drops every event (1000x off) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | raw parquet inputs | mitigate | `load_data` only reads via `pl.read_parquet(use_pyarrow=False)`; no write/rename to raw inputs anywhere in `main.py` |
| T-01-11 | Information disclosure / data loss | us↔ns lookup mismatch | mitigate | force `.astype("datetime64[ns]")` on lookup + `times` arrays; acceptance gate fails on N==0 rows |
| T-01-12 | Tampering | output contract dtype drift | mitigate | construct output with explicit `schema=CONTRACT_SCHEMA`; smoke `--check contract` asserts all 21 dtypes |
| T-01-13 | Tampering | methodology kernel | mitigate | kernel ported verbatim; manual spot-check confirms only boundary/scalar edits |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `python3 main.py` → `Successfully analyzed N events` with N>0.
- `python3 smoke/phase1_smoke.py --check contract` exits 0.
- `python3 smoke/phase1_smoke.py --check read` exits 0.
- `! grep -nE "^[[:space:]]*(import pandas|from pandas)" main.py` returns nothing; no `nq.attrs`.
- Pandas `tests/` suite is NOT used as a gate (expected red until Phase 3).
</verification>

<success_criteria>
`main.py` runs the sweep pipeline end-to-end on polars, reads the raw inputs natively (ENV-02), regenerates `data/sweep_analysis_results.parquet` with the exact pinned 21-col CONTRACT_SCHEMA and N>0 rows, threads lookups explicitly with no `.attrs`, and preserves the numpy methodology kernel verbatim — with zero pandas imports.
</success_criteria>

<output>
Create `.planning/phases/01-primary-pipeline-on-polars/01-02-SUMMARY.md` when done.
</output>
