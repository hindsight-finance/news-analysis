---
phase: 01-primary-pipeline-on-polars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [smoke/phase1_smoke.py]
autonomous: true
requirements: [MIGRATE-01, MIGRATE-02, MIGRATE-03, ENV-02]

must_haves:
  truths:
    - "Running the smoke harness validates the 21-column CONTRACT_SCHEMA against data/sweep_analysis_results.parquet"
    - "The harness reads both raw inputs natively (use_pyarrow=False) with no pandas in the read path"
    - "The harness statically detects any `import pandas` in the three Phase-1 scripts"
    - "The harness detects presence/absence of the exploration and causal chart + CSV outputs"
  artifacts:
    - path: "smoke/phase1_smoke.py"
      provides: "Phase-1 integration smoke harness (contract / native-read / no-pandas / output checks)"
      min_lines: 60
      contains: "EXPECTED_SCHEMA"
  key_links:
    - from: "smoke/phase1_smoke.py"
      to: "data/sweep_analysis_results.parquet"
      via: "pl.read_parquet then schema assert"
      pattern: "read_parquet"
    - from: "smoke/phase1_smoke.py"
      to: "main.py / exploration.py / causal_analysis.py"
      via: "regex scan for import pandas"
      pattern: "import pandas|from pandas"
---

<objective>
Create the Phase-1 integration smoke harness — the validation signal for the whole phase — BEFORE any script is ported. The existing pandas-based `tests/` suite asserts pandas-only semantics (`.loc`, `.attrs`, `pd.Timestamp.value`) on the very functions being ported, so it is expected RED until Phase 3 (TEST-01) and MUST NOT be used as the Phase-1 gate. This harness is the Nyquist scaffold: every later port task validates against it.

Purpose: Give every downstream port task a deterministic, pandas-free automated check (21-col contract schema, native raw-parquet read, no-pandas static scan, chart/CSV output existence) that is independent of the pandas unit suite.
Output: `smoke/phase1_smoke.py` — a standalone, pandas-free Python script runnable as `python3 smoke/phase1_smoke.py --check {contract,read,nopandas,exploration,causal,all}`, kept SEPARATE from `tests/` so the default `pytest tests` run is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md
@.planning/phases/01-primary-pipeline-on-polars/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase-1 integration smoke harness (smoke/phase1_smoke.py)</name>
  <files>smoke/phase1_smoke.py</files>
  <read_first>
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md (Pattern 2 CONTRACT_SCHEMA = the exact 21 columns + dtypes; Pitfall 1 us/ns; "Validation Architecture" → Phase Requirements → Test Map for the exact smoke one-liners)
    - .planning/phases/01-primary-pipeline-on-polars/01-VALIDATION.md (Per-Task Verification Map and Wave 0 Requirements — the harness is the Wave-0 gap)
  </read_first>
  <action>
    Create a NEW directory `smoke/` (NOT under `tests/`) and write `smoke/phase1_smoke.py` importing ONLY `polars as pl`, `argparse`, `re`, `sys`, and `pathlib.Path` — no pandas anywhere in this file.

    Define a module-level `EXPECTED_SCHEMA` dict mapping each of the 21 contract columns to its expected polars dtype STRING, in the exact on-disk order: `event_type`=`String`, `event_datetime`=`Datetime(time_unit='ns', time_zone='UTC')`, `release_time`=`String`, `data_high`=`Float64`, `data_low`=`Float64`, `range`=`Float64`, `range_pct`=`Float64`, `first_sweep`=`String`, `time_to_first_sweep`=`Float64`, `opposite_swept`=`Boolean`, `time_to_opposite_sweep`=`Float64`, `synthetic_box_breached`=`Boolean`, `first_target_hit`=`String`, `mae_before_reversal`=`Float64`, `pre_candle_range_pct`=`Float64`, `pre_candle_volume`=`Float64`, `dist_from_midnight_open_pct`=`Float64`, `dist_from_6pm_open_pct`=`Float64`, `gap_6pm_pct`=`Float64`, `gap_6pm_direction`=`String`, `release_volume`=`Int64`. These dtype strings are the verified polars 1.40.1 `str(dtype)` outputs (the drift-prone ones are `event_datetime`, `pre_candle_volume`, `release_volume`).

    Implement these check functions, each raising `AssertionError` with a clear message on failure and returning `None` on success:
    - `check_contract(path="data/sweep_analysis_results.parquet")`: `pl.read_parquet(path)`, assert schema length == 21, assert ordered column names == `list(EXPECTED_SCHEMA)`, assert `str(dtype) == expected` for every column (so a us-drift on `event_datetime` or Int64-drift on `pre_candle_volume` fails loudly per Pitfall 2), and assert `height > 0` (catches the us/ns silent-drop, Pitfall 1).
    - `check_read()`: `pl.read_parquet("data/nq_1m.parquet", use_pyarrow=False)` and `pl.read_parquet("data/economic_events.parquet", use_pyarrow=False)` both succeed (ENV-02 native read). Do NOT write or modify these files — read only.
    - `check_nopandas(files=("main.py","exploration.py","causal_analysis.py"))`: for each file, scan lines with regex `^\s*(import pandas|from pandas)` and assert zero matches.
    - `check_exploration()`: assert `charts/exploration/summary_by_event.csv` exists and at least one `charts/exploration/*.png` exists.
    - `check_causal()`: assert `charts/causal/event_stats.csv` exists and at least one `charts/causal/*.png` exists.

    Add an argparse CLI with `--check` taking one of `contract|read|nopandas|exploration|causal|all` (default `all`). Each value runs the corresponding function(s); `all` runs every check. On success print `SMOKE OK: <check>` and `sys.exit(0)`; on `AssertionError` print `SMOKE FAIL: <message>` and `sys.exit(1)`. The harness must be safe to run from the project root.

    Do NOT touch `tests/`, do NOT add a `conftest.py`, and do NOT import any of the three scripts (the static no-pandas check reads them as text, it does not import them).
  </action>
  <verify>
    <automated>python3 smoke/phase1_smoke.py --check contract && python3 smoke/phase1_smoke.py --check read && ! grep -nE "import pandas|from pandas" smoke/phase1_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 smoke/phase1_smoke.py --check contract` exits 0 against the EXISTING on-disk `data/sweep_analysis_results.parquet` (proves the encoded EXPECTED_SCHEMA matches the real 21-col contract: 21 cols, `event_datetime`=`Datetime(time_unit='ns', time_zone='UTC')`, `pre_candle_volume`=`Float64`, `release_volume`=`Int64`, height>0).
    - `python3 smoke/phase1_smoke.py --check read` exits 0 (both raw inputs read natively with `use_pyarrow=False`).
    - `python3 smoke/phase1_smoke.py --check nopandas` currently exits 1 (the three scripts still import pandas at Wave 1 — this is EXPECTED and proves the static scan works; it will flip to 0 as scripts are ported in later waves).
    - `! grep -nE "import pandas|from pandas" smoke/phase1_smoke.py` returns nothing (the harness itself is pandas-free).
    - The file `smoke/phase1_smoke.py` defines a module-level `EXPECTED_SCHEMA` with exactly 21 entries; `smoke/` is a new directory distinct from `tests/`.
  </acceptance_criteria>
  <done>The integration smoke harness exists, is pandas-free, validates the real 21-col contract and native reads today, and exposes per-check CLI entry points the later port tasks will gate on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → raw parquet inputs | `nq_1m.parquet` / `economic_events.parquet` are irreplaceable, read-only; the harness must only READ them |
| harness → contract artifact | `sweep_analysis_results.parquet` is the regenerated data contract the harness validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | raw parquet inputs | mitigate | harness opens raw inputs read-only via `pl.read_parquet`; no write/rename/delete calls in `smoke/phase1_smoke.py` |
| T-01-02 | Information disclosure / false-green | contract schema check | mitigate | EXPECTED_SCHEMA asserts all 21 names + dtype strings (not just count) so a silent us/Int64 dtype drift cannot pass |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase (RESEARCH Package Legitimacy Audit = N/A); all deps pre-installed and pinned |
</threat_model>

<verification>
- `python3 smoke/phase1_smoke.py --check contract` exits 0 against the existing contract.
- `python3 smoke/phase1_smoke.py --check read` exits 0.
- `python3 smoke/phase1_smoke.py --check nopandas` exits 1 at Wave 1 (expected; flips to 0 after the script ports).
- Harness imports no pandas; `tests/` is untouched.
</verification>

<success_criteria>
A standalone, pandas-free smoke harness exists under `smoke/`, independently encodes the 21-col CONTRACT_SCHEMA, validates the live contract + native reads today, and provides the per-check automated signal every later Phase-1 port task depends on. The pandas `tests/` suite is NOT ported and NOT used as a gate.
</success_criteria>

<output>
Create `.planning/phases/01-primary-pipeline-on-polars/01-01-SUMMARY.md` when done.
</output>
