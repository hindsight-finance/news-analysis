---
phase: 01-primary-pipeline-on-polars
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [exploration.py]
autonomous: true
requirements: [MIGRATE-02]

must_haves:
  truths:
    - "python3 exploration.py runs on polars consuming the polars-produced contract with zero pandas imports"
    - "It writes the win-rate, release-timing, range-quartile, and MAE chart outputs plus summary_by_event.csv"
    - "Win-rate / quartile aggregations use polars group_by/agg and qcut(allow_duplicates=True)"
  artifacts:
    - path: "exploration.py"
      provides: "Polars exploration script: compute_win_rates, qcut helper, build_summary_table, plots, run()"
    - path: "charts/exploration/summary_by_event.csv"
      provides: "Per-event summary table (regenerated by the polars run)"
  key_links:
    - from: "exploration.py run()"
      to: "data/sweep_analysis_results.parquet"
      via: "pl.read_parquet"
      pattern: "read_parquet"
    - from: "exploration.py plot_* / run()"
      to: "matplotlib"
      via: "polars Series .to_numpy()/.to_list() at the chart boundary"
      pattern: "to_numpy|to_list"
---

<objective>
Port `exploration.py` (consumer #1) from pandas to polars (MIGRATE-02). It reads the polars-produced `data/sweep_analysis_results.parquet` contract, computes win-rate / release-timing / range-quartile / MAE aggregations in polars, and writes the same chart PNGs and `summary_by_event.csv`. Charts stay on matplotlib, fed via `.to_numpy()`/`.to_list()` at the boundary.

Purpose: exploration.py is one of the two scripts whose correctness depends on the 21-col contract staying intact; porting it on top of the pinned contract proves the contract holds and the exploration outputs survive the engine swap.
Output: A polars `exploration.py` that regenerates all exploration charts + the summary CSV with zero pandas imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md
@.planning/phases/01-primary-pipeline-on-polars/01-VALIDATION.md
@exploration.py
@smoke/phase1_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the exploration aggregation layer (compute_win_rates, qcut helper, build_summary_table)</name>
  <files>exploration.py</files>
  <read_first>
    - exploration.py (current pandas implementation — read fully; especially compute_win_rates 23-42, qcut_with_fallback_labels 45-52, build_summary_table 55-86)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md ("Port compute_win_rates group aggregation" code example; Pitfall 4 qcut dead fallback; Pitfall 5 group_by order + value_counts; Pitfall 6 fill_null)
    - smoke/phase1_smoke.py (the --check exploration / --check nopandas signals the finished port must satisfy)
  </read_first>
  <action>
    Replace `import pandas as pd` with `import polars as pl` (keep matplotlib `Agg` set before pyplot, `argparse`, `pathlib`).

    Port `compute_win_rates(df, group_cols, min_count)` to polars per the RESEARCH example: `df.group_by(group_cols).agg(pl.len().alias("total"), (pl.col("first_target_hit")=="box").sum().alias("momentum_wins"), (pl.col("first_target_hit")=="opposite").sum().alias("reversal_wins"))`, then `.with_columns((momentum_wins+reversal_wins).alias("resolved"))`, `.filter(pl.col("total")>=min_count)`, and `.with_columns(pl.when(pl.col("resolved")>0).then(... /resolved*100).otherwise(None).alias("momentum_rate"))` and the same for `reversal_rate` (the `pl.when/then/otherwise` replaces pandas `.where`). polars `group_by` keeps null groups by default, matching the current `dropna=False`. Return a polars DataFrame exposing at least `total, resolved, momentum_rate, reversal_rate` plus the group columns.

    Port `qcut_with_fallback_labels(series, q, labels)` to `series.qcut(q, labels=labels, allow_duplicates=True)` (Pitfall 4: polars never raises on collapsed bins, so the pandas `except ValueError` fallback is unreachable — a single-line body is correct; these bins are display-only, not methodology-critical, so exact edges need not match pandas). Accept and return polars Series.

    Port `build_summary_table(df)` to polars: `df.group_by("event_type").agg(pl.len().alias("n"), (pl.col("first_target_hit")=="box").sum().alias("momentum"), (pl.col("first_target_hit")=="opposite").sum().alias("reversal"), pl.col("mae_before_reversal").mean().alias("avg_mae"), pl.col("mae_before_reversal").median().alias("median_mae"), pl.col("time_to_first_sweep").mean().alias("avg_time_to_sweep"), pl.col("range_pct").mean().alias("avg_range_pct"))`, then `.with_columns` for `resolved`, `momentum_rate`, `reversal_rate` (rounded), `edge` = `(momentum_rate-50).abs()`, then `.sort("edge", descending=True)` and `.select([...])` the 8 output columns in the same order as today.

    Document (in a short comment above each) the polars columns each function returns so Task 2's plots/run can index them. Do NOT yet touch the plot functions or `run()` — Task 2 ports those.
  </action>
  <verify>
    <automated>python3 -c "import exploration as e, polars as pl; df=pl.read_parquet('data/sweep_analysis_results.parquet'); wr=e.compute_win_rates(df,['event_type']); assert isinstance(wr, pl.DataFrame) and {'total','resolved','momentum_rate','reversal_rate'} <= set(wr.columns); st=e.build_summary_table(df); assert isinstance(st, pl.DataFrame) and st.height>0; print('agg OK', wr.height, st.height)"</automated>
  </verify>
  <acceptance_criteria>
    - `compute_win_rates(df, ["event_type"])` returns a polars DataFrame containing `total`, `resolved`, `momentum_rate`, `reversal_rate`.
    - `qcut_with_fallback_labels` calls `Series.qcut(..., allow_duplicates=True)` (verify `grep -n "allow_duplicates=True" exploration.py`) and returns a polars Series without raising on the contract's `range_pct` / `time_to_first_sweep` columns.
    - `build_summary_table(df)` returns a polars DataFrame sorted by `edge` descending with the original 8 output columns.
    - `! grep -nE "groupby|\.where\(|pd\.qcut" exploration.py` returns nothing in the ported functions (no pandas idioms left in the aggregation layer).
  </acceptance_criteria>
  <done>The exploration aggregation layer is ported to polars with documented return contracts ready for the plot/run wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Port the exploration plot functions + run() orchestration to polars</name>
  <files>exploration.py</files>
  <read_first>
    - exploration.py (current plot_* functions 89-173 and run() 176-228 — read fully, especially the value_counts idiom at 184-185 and the merge/notna idioms)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md (Pitfall 5 value_counts→2-col DataFrame with the exact rewrite; "CSV / clip / one-hot ports" example for write_csv and clip(upper_bound=))
    - smoke/phase1_smoke.py (--check exploration asserts the CSV + PNG outputs)
  </read_first>
  <action>
    Port the `plot_*` functions to feed matplotlib numpy/lists at the boundary: replace each `df["col"]`/Series passed to `ax.bar`/`ax.barh`/`ax.hist`/tick labels with `df.get_column("col").to_numpy()` or `.to_list()`. Replace `.clip(upper=10)` / `.clip(upper=5)` with polars `Series.clip(upper_bound=10)` / `(upper_bound=5)` then `.to_numpy()`. Keep figure layout, colors, titles, and OUTPUT FILENAMES identical (`momentum_vs_reversal_by_event.png`, `momentum_vs_reversal_by_release_time.png`, `win_rate_by_range_quartile.png`, `mae_distribution.png`).

    Port `run()`: `df = pl.read_parquet(input_path)`. Rewrite the common-times block (Pitfall 5: `value_counts()` now returns a 2-col DataFrame) as `vc = df["release_time"].value_counts(); common_times = vc.filter(pl.col("count")>=10)["release_time"].to_list()`, then filter via `df.filter(pl.col("release_time").is_in(common_times))`. Replace every boolean-mask access `df[df["x"]==v]` with `df.filter(pl.col("x")==v)` and `.notna()` with `.is_not_null()`. Replace `.merge(other, on=..., how="left")` with `.join(other, on=..., how="left")`. Replace `groupby(...)["x"].mean()` with `group_by(...).agg(pl.col("x").mean().alias("avg_..."))`. Replace `to_csv(summary_path, index=False)` with `summary.write_csv(summary_path)`. For the per-event block and printed tables, use polars idioms (`.iter_rows(named=True)` or `to_numpy()`); the `range_quartile`/`time_quartile` columns come from the ported `qcut_with_fallback_labels` (Categorical) — `group_by` on them shows only occurring bins (acceptable, display-only). Remove the pandas import entirely.
  </action>
  <verify>
    <automated>python3 exploration.py && test -f charts/exploration/summary_by_event.csv && ls charts/exploration/*.png >/dev/null && python3 smoke/phase1_smoke.py --check exploration && ! grep -nE "^[[:space:]]*(import pandas|from pandas)" exploration.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 exploration.py` runs to completion (consuming the polars-produced contract) without error.
    - `charts/exploration/summary_by_event.csv` exists and at least one `charts/exploration/*.png` exists.
    - `python3 smoke/phase1_smoke.py --check exploration` exits 0.
    - `! grep -nE "^[[:space:]]*(import pandas|from pandas)" exploration.py` returns nothing; `! grep -nE "\.merge\(|\.iterrows\(|\.notna\(|pd\." exploration.py` returns nothing.
  </acceptance_criteria>
  <done>`exploration.py` runs end-to-end on polars, regenerates all exploration charts + `summary_by_event.csv`, and contains zero pandas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exploration.py → contract artifact | reads `sweep_analysis_results.parquet`; correctness depends on the 21-col contract |
| exploration.py → chart/CSV outputs | regenerated PNG/CSV under `charts/exploration/` (overwrite-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-30 | Tampering | contract consumption | mitigate | reads the pinned contract via `pl.read_parquet`; `--check exploration` asserts outputs regenerate |
| T-01-31 | Repudiation / silent miscount | value_counts/group_by order | accept | printed-table order is cosmetic (Pitfall 5); win-rate values are computed via explicit agg, not order-dependent |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `python3 exploration.py && test -f charts/exploration/summary_by_event.csv && ls charts/exploration/*.png` succeeds.
- `python3 smoke/phase1_smoke.py --check exploration` exits 0.
- `! grep -nE "^[[:space:]]*(import pandas|from pandas)" exploration.py` returns nothing.
</verification>

<success_criteria>
`exploration.py` runs on polars against the pinned contract, regenerates its win-rate / release-timing / range-quartile / MAE charts and `summary_by_event.csv`, and contains zero pandas imports.
</success_criteria>

<output>
Create `.planning/phases/01-primary-pipeline-on-polars/01-03-SUMMARY.md` when done.
</output>
