---
phase: 01-primary-pipeline-on-polars
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified: [causal_analysis.py]
autonomous: true
requirements: [MIGRATE-03]

must_haves:
  truths:
    - "python3 causal_analysis.py runs on polars for data handling with zero pandas imports"
    - "scikit-learn models are trained via an explicit polars->numpy conversion at the .fit()/cross_val_score boundary"
    - "feature column names are retained from the polars feature frame for importance/coefficient/tree labels"
    - "It writes charts/causal/event_stats.csv plus the feature-importance, logistic, tree, and event-edge charts"
  artifacts:
    - path: "causal_analysis.py"
      provides: "Polars causal script: load_resolved_results, build_features (returns polars frame), run() with explicit numpy boundary"
    - path: "charts/causal/event_stats.csv"
      provides: "Per-event edge ranking (regenerated by the polars run)"
  key_links:
    - from: "causal_analysis.py build_features"
      to: "run() model training"
      via: "features.to_numpy() / y.to_numpy() converted ONCE at the sklearn boundary"
      pattern: "to_numpy"
    - from: "causal_analysis.py run()"
      to: "data/sweep_analysis_results.parquet"
      via: "pl.read_parquet then filter to resolved rows"
      pattern: "read_parquet"
---

<objective>
Port `causal_analysis.py` (consumer #2) from pandas to polars (MIGRATE-03). Keep ALL data handling in polars and convert to numpy exactly ONCE, at the scikit-learn boundary (`.fit()` / `cross_val_score` / `StandardScaler` / `LabelEncoder`), retaining the polars feature column names for importance, coefficient, and decision-tree labels. It reads the polars-produced contract, engineers features, trains RF / DecisionTree / LogisticRegression, and writes `event_stats.csv` plus the causal charts.

Purpose: causal_analysis.py is the second contract consumer and the explicit home of the MIGRATE-03 `polars -> numpy` model boundary. Doing the conversion once, at `.fit()`, is the defining requirement of this plan.
Output: A polars `causal_analysis.py` that trains its models on numpy arrays produced by `features.to_numpy()` and regenerates all causal outputs with zero pandas imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md
@.planning/phases/01-primary-pipeline-on-polars/01-VALIDATION.md
@causal_analysis.py
@smoke/phase1_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the causal data + feature layer (load_resolved_results, build_features returns polars, qcut, cv_folds)</name>
  <files>causal_analysis.py</files>
  <read_first>
    - causal_analysis.py (current pandas implementation — read fully; especially qcut_with_fallback_labels 31-38, load_resolved_results 41-45, build_features 48-73, cv_folds 76-80)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md ("Port build_features gap-direction map" example with replace_strict; Pattern 3 polars->numpy boundary; Pitfall 4 qcut; Pitfall 6 fill_null != fillna; "Categorical->int encoding for models" LabelEncoder on .to_numpy())
    - smoke/phase1_smoke.py (--check causal / --check nopandas signals the finished port must satisfy)
  </read_first>
  <action>
    Replace `import pandas as pd` with `import polars as pl` (keep `numpy as np`, sklearn imports, matplotlib `Agg`, and `warnings.filterwarnings("ignore")` unchanged — QUAL-01 scoping is deferred).

    Port `qcut_with_fallback_labels(series, q, labels)` to `series.qcut(q, labels=labels, allow_duplicates=True)` (Pitfall 4; display-only bins).

    Port `load_resolved_results(input_path)`: `df = pl.read_parquet(input_path)`; `df = df.filter(pl.col("first_target_hit").is_not_null())`; add target via `df = df.with_columns((pl.col("first_target_hit")=="box").cast(pl.Int64).alias("target"))`; return the polars DataFrame.

    Port `build_features(df)` to RETURN `(features: pl.DataFrame, y: pl.Series)`, building `features` with `with_columns`/`select` in the SAME column order/names as today so `features.columns` stays a stable label list: `event_type_encoded` = `LabelEncoder().fit_transform(df.get_column("event_type").to_numpy())` wrapped back as a polars column; `release_time_encoded` likewise on `release_time`; `first_sweep_high` = `(pl.col("first_sweep")=="high").cast(pl.Int64)`; `gap_direction_encoded` = `pl.col("gap_6pm_direction").replace_strict({"up":1,"down":0,"flat":-1}, default=-1, return_dtype=pl.Int64)`; passthrough numerics `range_pct`, `release_volume`, `time_to_first_sweep`; and `fill_null(0)` (Pitfall 6 — NOT fillna; the session-context missing values are nulls) on `pre_candle_range_pct`, `pre_candle_volume`, `dist_from_midnight_open_pct`, `dist_from_6pm_open_pct`, `gap_6pm_pct`. Keep the exact 12 feature columns in the original order. Set `y = df.get_column("target")`.

    Port `cv_folds(y)` to use polars: derive the per-class minimum from `y.value_counts()` (a 2-col DataFrame `(target, count)`) — `int(max(0, min(desired, <min count column value>)))`; return 0 when empty. Do NOT yet rewrite `run()`/plots — Task 2 does the model boundary.
  </action>
  <verify>
    <automated>python3 -c "import causal_analysis as c, polars as pl; df=c.load_resolved_results('data/sweep_analysis_results.parquet'); assert isinstance(df, pl.DataFrame) and 'target' in df.columns; f,y=c.build_features(df); assert isinstance(f, pl.DataFrame) and f.width==12 and isinstance(y, pl.Series); print('features OK', f.width, y.len())"</automated>
  </verify>
  <acceptance_criteria>
    - `load_resolved_results(path)` returns a polars DataFrame filtered to resolved rows (`first_target_hit` not null) with an integer `target` column.
    - `build_features(df)` returns `(pl.DataFrame, pl.Series)` where the DataFrame has exactly the 12 feature columns in the original order; `grep -n "replace_strict" causal_analysis.py` confirms the gap-direction map; `grep -n "fill_null" causal_analysis.py` confirms null filling (not `fillna`).
    - No NaN leaks from the null fills (the filled columns originate as polars nulls, so `fill_null(0)` covers them).
    - `! grep -nE "pd\.qcut|\.fillna\(|pd\.DataFrame" causal_analysis.py` returns nothing in the ported functions.
  </acceptance_criteria>
  <done>The causal data + feature layer is ported to polars; `build_features` returns a polars feature frame + target with stable column names, ready for the numpy boundary in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Port run() model training (explicit polars->numpy boundary) + charts + event_stats.csv</name>
  <files>causal_analysis.py</files>
  <read_first>
    - causal_analysis.py (current run() 139-260 and plot_* 92-136 — read fully; especially the model .fit() calls, StandardScaler, get_dummies readable tree, correlations, event_stats group/agg, and the print loops)
    - .planning/phases/01-primary-pipeline-on-polars/01-RESEARCH.md (Pattern 3 explicit polars->numpy at .fit()/cross_val_score; "CSV / clip / one-hot ports" for to_dummies and write_csv; Assumptions A2 to_dummies column-name change is print-only)
    - smoke/phase1_smoke.py (--check causal asserts event_stats.csv + the PNG charts)
  </read_first>
  <action>
    Port `run()`. After `features, y = build_features(df)`, establish the SINGLE numpy boundary (MIGRATE-03): `feature_names = features.columns`; `X = features.to_numpy()`; `y_np = y.to_numpy()`. Feed `X, y_np` to every estimator — `rf.fit(X, y_np)`, `cross_val_score(model, X, y_np, cv=folds)` (inside `print_cv_score`, which must also take numpy or convert at its boundary), `tree.fit(X, y_np)`, `X_scaled = StandardScaler().fit_transform(X)`, `lr.fit(X_scaled, y_np)`. Do NOT pass polars frames into sklearn.

    Build `importance` and `coefs` as polars DataFrames using `feature_names` aligned positionally to `rf.feature_importances_` and `lr.coef_[0]` (e.g. `pl.DataFrame({"feature": feature_names, "importance": rf.feature_importances_}).sort("importance")`). The `plot_feature_importance` / `plot_logistic_coefficients` / `plot_event_edges` functions feed matplotlib via `.to_numpy()`/`.to_list()` at the boundary; keep figure layout, colors, titles, and OUTPUT FILENAMES identical (`feature_importance.png`, `logistic_coefficients.png`, `decision_tree.png`, `event_edge.png`).

    Port the correlations block: compute each numeric feature's correlation with the target — either `features.select(pl.corr(pl.col(f), pl.lit(y_np))...)` per feature or numpy `float(np.corrcoef(features.get_column(f).to_numpy(), y_np)[0,1])`; keep the `numeric_features` list and build a polars `correlations` DataFrame sorted ascending.

    Port `event_stats`: `df.group_by("event_type").agg(pl.len().alias("n"), pl.col("target").mean().alias("momentum_rate"), pl.col("range_pct").mean().alias("avg_range"), pl.col("release_volume").mean().alias("avg_volume"), pl.col("dist_from_midnight_open_pct").mean().alias("avg_dist_midnight"))`, then `.with_columns` for `reversal_rate`=1-momentum_rate, `edge`=`(momentum_rate-0.5).abs()*100`, and `direction` via `pl.when(pl.col("momentum_rate")>0.5).then(pl.lit("MOMENTUM")).otherwise(pl.lit("REVERSAL"))`; `.sort("edge", descending=True)`; write with `event_stats.write_csv(output_dir / "event_stats.csv")`. Port `gap_stats` / `midnight_stats` group_by/agg the same way (the `midnight_dist_quartile` comes from the ported `qcut_with_fallback_labels`).

    Port the readable decision tree: replace `pd.get_dummies(df["event_type"], prefix="event")` with `df.select("event_type").to_dummies(columns=["event_type"])` (note column names become `event_type_X` rather than `event_X` — print-only, Assumptions A2), horizontally combine with the selected feature columns (`pl.concat([...], how="horizontal")` or build one frame), convert with `.to_numpy()` at `tree_readable.fit(...)`, and pass `feature_names=<combined column list>` to `export_text`. Replace the `.iterrows()` print loops over `importance` / `event_stats` / `correlations` with `.iter_rows(named=True)`. Remove the pandas import entirely.
  </action>
  <verify>
    <automated>python3 causal_analysis.py && test -f charts/causal/event_stats.csv && ls charts/causal/*.png >/dev/null && python3 smoke/phase1_smoke.py --check causal && ! grep -nE "^[[:space:]]*(import pandas|from pandas)" causal_analysis.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 causal_analysis.py` runs to completion (consuming the polars-produced contract) without error.
    - The model boundary is explicit: `grep -nE "features\.to_numpy\(\)|X = features.to_numpy" causal_analysis.py` shows the single conversion, and estimators receive `X`/`y_np` numpy arrays (no polars frame passed to `.fit(`/`cross_val_score`).
    - `charts/causal/event_stats.csv` exists and at least one `charts/causal/*.png` exists.
    - `python3 smoke/phase1_smoke.py --check causal` exits 0.
    - `! grep -nE "^[[:space:]]*(import pandas|from pandas)" causal_analysis.py` returns nothing; `! grep -nE "pd\.get_dummies|\.iterrows\(|pd\." causal_analysis.py` returns nothing.
  </acceptance_criteria>
  <done>`causal_analysis.py` runs end-to-end on polars, trains its sklearn models via a single explicit `features.to_numpy()` boundary with retained feature names, regenerates `event_stats.csv` + all causal charts, and contains zero pandas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| causal_analysis.py → contract artifact | reads `sweep_analysis_results.parquet`; correctness depends on the 21-col contract |
| polars → numpy → scikit-learn | the model boundary; arrays must cross once, cleanly, with aligned feature names |
| causal_analysis.py → chart/CSV outputs | regenerated PNG/CSV under `charts/causal/` (overwrite-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-40 | Tampering | feature/label misalignment | mitigate | `feature_names = features.columns` captured before `to_numpy()`; importance/coef/tree labels index it positionally |
| T-01-41 | Information disclosure / NaN leak | null handling into models | mitigate | `fill_null(0)` (Pitfall 6) on nullable session-context features before `to_numpy()` |
| T-01-42 | Tampering | contract consumption | mitigate | reads the pinned contract via `pl.read_parquet`; `--check causal` asserts outputs regenerate |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `python3 causal_analysis.py && test -f charts/causal/event_stats.csv && ls charts/causal/*.png` succeeds.
- `python3 smoke/phase1_smoke.py --check causal` exits 0.
- `! grep -nE "^[[:space:]]*(import pandas|from pandas)" causal_analysis.py` returns nothing.
- The polars->numpy conversion happens once, at the model boundary.
</verification>

<success_criteria>
`causal_analysis.py` runs on polars against the pinned contract, trains its scikit-learn models via an explicit single `features.to_numpy()` / `y.to_numpy()` conversion at `.fit()`/`cross_val_score` with retained feature-name labels, regenerates `event_stats.csv` and all causal charts, and contains zero pandas imports.
</success_criteria>

<output>
Create `.planning/phases/01-primary-pipeline-on-polars/01-04-SUMMARY.md` when done.
</output>
