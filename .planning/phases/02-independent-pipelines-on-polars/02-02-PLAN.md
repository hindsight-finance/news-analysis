---
phase: 02-independent-pipelines-on-polars
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [injection.py]
autonomous: true
requirements: [MIGRATE-05]

must_haves:
  truths:
    - "python3 injection.py exits 0 and writes per-event histogram PNGs to charts/ (ROADMAP criterion 3)"
    - "injection.py release-candle range/volume and 10-minute range are computed in polars (ROADMAP criterion 3)"
    - "injection.py release-candle lookup uses a pure-polars exact-match lookup, replacing the linear boolean-mask scan (D-02)"
    - "Release-candle lookup is exact-match-or-None — no join_asof / nearest-match (D-04)"
    - "The 10-minute window is a TIME-bounded filter on DateTime_UTC inclusive [event_time, event_time+9min], NOT a positional +9-row slice (D-06)"
    - "nq DateTime_UTC and event datetime are normalized to ns/UTC before equality, so events are NOT silently dropped (D-05)"
    - "matplotlib.use('Agg') appears before import matplotlib.pyplot (D-10)"
    - "injection.py keeps its main() entry shape — no run()/argparse added (D-11)"
  artifacts:
    - path: "injection.py"
      provides: "polars port of the injection range/histogram pipeline; Agg-before-pyplot; pure-polars exact-match lookup"
      contains: "matplotlib.use(\"Agg\")"
    - path: "charts"
      provides: "per-event release-candle + 10-minute range histogram PNGs"
  key_links:
    - from: "injection.py"
      to: "data/nq_1m.parquet"
      via: "pl.read_parquet(use_pyarrow=False)"
      pattern: "pl\\.read_parquet"
    - from: "injection.py get_release_candle_data"
      to: "nq DateTime_UTC row index"
      via: "with_row_index + inner-join exact-match resolution"
      pattern: "with_row_index"
---

<objective>
Port `injection.py` from pandas to polars (MIGRATE-05): upgrade the release-candle lookup from its
slow linear boolean-mask scan to the same pure-polars exact-match lookup used by forward_returns.py
(D-02/D-03), keep the 10-minute window a TIME-bounded filter (D-06), add the headless `Agg` guard
(D-10), and keep the existing `main()` entry shape — do NOT add run()/argparse (D-11, STRUCT-03 is
deferred). The release-candle range/volume and 10-minute range methodology must survive intact:
exact-match-or-None lookup (D-04) and ns/UTC precision normalization before equality (D-05).

Purpose: injection.py is the second independent raw-data pipeline this phase moves onto polars. Its
current `nq[nq['DateTime_UTC'] == event_time]` mask is the documented linear-scan anti-pattern; at
5.3M nq rows it is replaced by a fast pure-polars exact-match lookup.

Output: a pandas-free injection.py with the Agg guard, a pure-polars exact-match release lookup, a
time-bounded 10-minute window, and its original main() shape, producing the per-event histogram PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md
@.planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md
@injection.py
@main.py
@exploration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port injection.py imports, load_data, exact-match lookup, and range functions</name>
  <files>injection.py</files>
  <read_first>
    - injection.py — current pandas implementation. Read `load_data` (the tz handling being replaced), `calculate_percentage_range` (pure scalar math, carry over), `get_release_candle_data` (the linear boolean-mask anti-pattern being upgraded), `get_10min_range` (the time-bounded window to preserve), and the import block (no Agg guard today).
    - main.py — `load_data()` (~85-119): the polars tz/precision normalization template (rename datetime_utc to DateTime_UTC, branch on time_zone, sort) and `nq.row(idx, named=True)` single-row extraction (~128, ~138).
    - .planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md — "Shared Patterns -> Pure-polars exact-match lookup" (same construct as forward_returns), injection section 1 (load), section 2 (lookup, D-02/D-04), section 3 (10-min time window, D-06), section 4 (Agg, D-10).
    - .planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md — D-02, D-03, D-04, D-05, D-06, D-10.
  </read_first>
  <action>
    Fix the import block (D-10): add `import matplotlib` then `matplotlib.use("Agg")` BEFORE `import matplotlib.pyplot as plt`, matching every other script. Replace `import pandas as pd` with `import polars as pl`; KEEP `import numpy as np` (np.median/np.mean in the histograms). Keep the module-level `DATA_DIR`/`OUTPUT_DIR` and `OUTPUT_DIR.mkdir(exist_ok=True)` as-is (CWD/path cleanup is deferred STRUCT work, out of scope).

    Port `load_data` to polars mirroring main.py `load_data()`: read both inputs via `pl.read_parquet(path, use_pyarrow=False)`; rename `datetime_utc` to `DateTime_UTC` on nq if needed (the on-disk column IS `datetime_utc`; the current `nq['DateTime_UTC']` access is a latent bug — fix it with the rename); normalize `DateTime_UTC` and the events `datetime_utc` to UTC (naive -> `dt.replace_time_zone("UTC")`, aware -> `dt.convert_time_zone("UTC")`). CRITICAL (D-05): nq is `Datetime(us, UTC)`, events is `Datetime(ns, UTC)` — force a common unit with `pl.col("DateTime_UTC").dt.cast_time_unit("ns")` on the nq side (and ensure the event key is ns/UTC) BEFORE any equality, or every `==` fails and all events silently drop. `nq = nq.sort("DateTime_UTC")`. Return `(events, nq)`.

    Implement the same pure-polars exact-match lookup as forward_returns.py (D-02/D-03/D-04): build `nq_idx = nq.with_row_index("idx")` once, and resolve a target timestamp to a row index via an inner-join / equality against `nq_idx.select(["DateTime_UTC", "idx"])` (exact-match -> miss drops, reproducing the old `if candle.empty: return None`, D-04; NO join_asof). A per-event O(n) `.filter(== ts)` over the 5.3M-row frame is the slow-scan anti-pattern D-02 exists to remove — resolve indices via the join (e.g. resolve all event release indices in one inner-join, collapsed into a python timestamp->idx dict).

    Port `get_release_candle_data` to use that exact-match lookup: resolve the release row index for `event_time`; on miss `return None` (D-04); else `candle = nq.row(idx, named=True)`; `pct_range = calculate_percentage_range(candle["High"], candle["Low"], candle["Open"])`; return `(pct_range, candle["Volume"])`. Carry `calculate_percentage_range` over verbatim.

    Port `get_10min_range` keeping it TIME-bounded (D-06, do NOT make it a positional +9-row slice): `end_time = event_time + timedelta(minutes=9)`; `candles = nq.filter(pl.col("DateTime_UTC").is_between(event_time, end_time, closed="both"))` (inclusive both ends, matching the old `>= event_time & <= end_time`); `if candles.is_empty(): return None`; `high = candles.get_column("High").max()`, `low = candles.get_column("Low").min()`, `reference_price = candles.row(0, named=True)["Open"]` (first candle by time — nq is sorted); return `calculate_percentage_range(high, low, reference_price)`. (Optional fast-path: prefilter a bounded neighborhood via the resolved release idx before the time filter, since a 10-minute inclusive window holds at most 10 one-minute candles — but the window membership MUST remain defined by the time predicate, not a row count, per D-06.)
  </action>
  <acceptance_criteria>
    - `injection.py` contains `matplotlib.use("Agg")` and it appears BEFORE `import matplotlib.pyplot` (D-10).
    - `grep -nE "import pandas|from pandas" injection.py` returns NO matches; `import numpy as np` remains.
    - `get_release_candle_data` uses the pure-polars exact-match lookup (`with_row_index` present; no `nq[nq[...] == ...]` boolean-mask scan), returns None on miss (D-02, D-04).
    - `get_10min_range` is a time filter using `is_between` on `DateTime_UTC` (D-06) — NOT `iloc`/`slice` of +9 rows.
    - Loading the real data and calling get_release_candle_data / get_10min_range for a known event minute returns non-None (proves D-05 ns/UTC normalization holds).
  </acceptance_criteria>
  <verify>
    <automated>! grep -nE "import pandas|from pandas" injection.py && grep -q "with_row_index" injection.py && grep -q "is_between" injection.py && python3 -c "import re,sys; s=open('injection.py').read(); a=s.index('matplotlib.use(\"Agg\")'); b=s.index('import matplotlib.pyplot'); assert a<b, 'Agg not before pyplot (D-10)'; print('Agg before pyplot OK')" && python3 -c "import polars as pl, injection as inj; ev, nq = inj.load_data(); m = ev.join(nq.select(pl.col(\"DateTime_UTC\").alias(\"datetime_utc\")), on=\"datetime_utc\", how=\"inner\").height; assert m > 0, \"D-05 broken: 0 event minutes match nq -- all events would silently drop\"; print(\"D-05 OK:\", m, \"event minutes match nq\")"</automated>
  </verify>
  <done>injection.py imports polars with the Agg-before-pyplot guard; load_data normalizes both sides to ns/UTC (D-05); get_release_candle_data uses the pure-polars exact-match lookup replacing the linear scan (D-02/D-04); get_10min_range stays a time-bounded is_between filter (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Port injection.py main() loop and create_histograms to polars (keep main() shape)</name>
  <files>injection.py</files>
  <read_first>
    - injection.py — `main()` (the per-event-type loop using `events['title'].unique()`, `events[events['title']==name]`, `iterrows()`) and `create_histograms` (np.median/np.mean/ax.hist over python lists). Read the `if __name__ == "__main__": main()` block (must stay).
    - exploration.py / causal_analysis.py — the polars->numpy/list boundary for matplotlib (`.to_numpy()`/`.to_list()`); `main.py` `events.iter_rows(named=True)` (~355) for the per-event loop.
    - .planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md — injection section 5 (histogram boundary), section 6 (entry point, D-11), and the pandas->polars idiom-swap table.
    - .planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md — D-11.
  </read_first>
  <action>
    Port `main()` body to polars while KEEPING its shape (D-11 — do NOT add run()/argparse, do NOT restructure the entry point; STRUCT-03 is deferred): `events, nq = load_data()`; build `nq_idx = nq.with_row_index("idx")` once after load and thread it to the release lookup. Replace `events['title'].unique()` with `events.get_column("title").unique().to_list()`; replace `events[events['title'] == event_name]` with `events.filter(pl.col("title") == event_name)`; replace `for _, row in event_occurrences.iterrows()` with `for row in event_occurrences.iter_rows(named=True)` and `event_time = row["datetime_utc"]`. Call `get_release_candle_data` (with the indexed nq) and `get_10min_range` (with nq) per occurrence; keep the existing None-guards and the `release_ranges`/`release_volumes`/`ten_min_ranges` python-list accumulation unchanged. Preserve the silent skip/None behavior in both helpers (VALID-02 is deferred — do NOT add skipped-event counting). Keep `if __name__ == "__main__": main()`.

    `create_histograms` needs minimal change: the accumulation lists stay plain python lists; `np.median(...)`, `np.mean(...)`, and `ax.hist(...)` over those lists are unchanged. The values appended already come back as python scalars from the helpers, so no extra polars->numpy conversion is required inside this function. Cosmetic touch-ups (double quotes, `from __future__ import annotations`) are NOT required (D-11) — keep the port focused.
  </action>
  <acceptance_criteria>
    - `python3 injection.py` exits 0 and writes per-event histogram PNGs into charts/ (one per event type that has matching price data).
    - `main()` is still defined and `if __name__ == "__main__": main()` remains; NO `argparse` / `def run(` was added (D-11).
    - The per-event loop uses `events.iter_rows(named=True)` and `events.filter(pl.col("title") == ...)`; `events.get_column("title").unique()` replaces the pandas `.unique()`.
    - `injection.py` has zero `import pandas`.
  </acceptance_criteria>
  <verify>
    <automated>python3 injection.py && ls charts/*.png >/dev/null && ! grep -nE "import pandas|from pandas|argparse|def run\(" injection.py && grep -q "def main" injection.py && grep -q "iter_rows" injection.py</automated>
  </verify>
  <done>injection.py runs end-to-end on polars, writes its per-event histogram PNGs, keeps the main() entry shape with no run()/argparse (D-11), and is pandas-free; range + histogram inputs are computed in polars (MIGRATE-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/*.parquet -> injection.py | Local, read-only, irreplaceable raw inputs cross into the analysis script. No network, no untrusted/user-supplied input, no package-manager installs in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure (silent data loss) | release-candle lookup in get_release_candle_data | mitigate | Cast nq DateTime_UTC to ns/UTC before equality (D-05); acceptance asserts a known event minute resolves non-None so a precision-mismatch drop is caught, not silently shipped. |
| T-02-05 | Tampering | data/nq_1m.parquet, data/economic_events.parquet | mitigate | Reads only via pl.read_parquet(use_pyarrow=False); never write back to data/ inputs (irreplaceable, gitignored — MEMORY constraint). |
| T-02-06 | Tampering (methodology corruption) | release lookup + 10-min window | mitigate | Exact-match-or-None release lookup, no join_asof (D-04); 10-min window stays a time-bounded is_between filter (D-06) so missing minutes/gaps do not change the window definition. |

No npm/pip/cargo install tasks in this plan (polars 1.40.1 already installed) -> no supply-chain (T-..-SC) checkpoint required.
</threat_model>

<verification>
- `python3 injection.py` exits 0 and writes per-event histogram PNGs to charts/ (ROADMAP criterion 3).
- `grep -nE "import pandas|from pandas" injection.py` returns no matches; Agg appears before pyplot (D-10).
- Release lookup uses pure-polars exact-match (`with_row_index`, no boolean-mask scan); returns None on miss (D-02, D-04).
- 10-minute window uses `is_between` time filter on DateTime_UTC (D-06), not a +9-row positional slice.
- No run()/argparse added; main() shape preserved (D-11).
- A known event minute resolves non-None (D-05 precision guard holds).
</verification>

<success_criteria>
injection.py runs entirely on polars with no pandas, has the Agg-before-pyplot guard, computes the
release-candle range/volume and 10-minute range in polars, uses a pure-polars exact-match lookup that
preserves D-04 (exact-match-or-None) and D-05 (ns/UTC precision), keeps the 10-minute window
time-bounded (D-06), retains its main() entry shape with no run()/argparse (D-11), and produces its
per-event histogram PNGs. MIGRATE-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-independent-pipelines-on-polars/02-02-SUMMARY.md` when done.
</output>
