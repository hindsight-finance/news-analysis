---
phase: 02-independent-pipelines-on-polars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [forward_returns.py]
autonomous: true
requirements: [MIGRATE-04]

must_haves:
  truths:
    - "python3 forward_returns.py exits 0 and writes charts/forward_returns/forward_returns_by_event.csv (ROADMAP criterion 1)"
    - "forward_returns.py writes per-horizon PNGs: raw_by_direction, direction_normalized, mae_mfe_by_direction, normalized_mae_mfe_scatter"
    - "forward_returns.py lookup path is pure-polars: no np.searchsorted and no import pandas anywhere in the file (ROADMAP criterion 2 / D-01)"
    - "Release and future candle lookups are exact-match — events whose exact minute is absent are skipped, no join_asof / nearest-match (D-04)"
    - "nq DateTime_UTC and event datetime are normalized to ns/UTC before any equality/join key, so events are NOT silently dropped (D-05)"
    - "Window aggregation preserves the positional [release_idx+1, future_idx] semantics via row index (D-07)"
    - "Summaries computed via polars group_by/agg; direction-normalized rows excluded via is_not_nan (D-08, D-09)"
  artifacts:
    - path: "forward_returns.py"
      provides: "polars port of the forward-returns pipeline; pure-polars exact-match lookup"
      contains: "pl.read_parquet"
    - path: "charts/forward_returns/forward_returns_by_event.csv"
      provides: "per-event/per-horizon forward-return rows written from the polars frame"
  key_links:
    - from: "forward_returns.py"
      to: "data/nq_1m.parquet"
      via: "pl.read_parquet(use_pyarrow=False)"
      pattern: "pl\\.read_parquet"
    - from: "forward_returns.py build_forward_returns"
      to: "nq DateTime_UTC row index"
      via: "with_row_index + inner-join exact-match resolution"
      pattern: "with_row_index"
---

<objective>
Port `forward_returns.py` from pandas to polars (MIGRATE-04), replacing the `np.searchsorted` /
`find_sorted_pos` timestamp lookup with a pure-polars exact-match lookup (D-01), and porting the
summary aggregations and charts to polars. The post-news-release forward-return methodology must
survive intact: exact-match-or-skip lookups (D-04), ns/UTC precision normalization before equality
(D-05), and the positional `[release_idx+1, future_idx]` window (D-07) are non-negotiable.

Purpose: forward_returns.py is one of the two independent raw-data pipelines this phase moves onto
polars. It currently uses numpy `searchsorted` for fast candle lookups; the user chose a true
pure-polars lookup over mirroring main.py's numpy pattern (D-01/D-03). main.py KEEPS its numpy
pattern — do not touch it, and do not revert this file to numpy for "consistency."

Output: a pandas-free, searchsorted-free forward_returns.py that runs end-to-end on polars and
produces forward_returns_by_event.csv plus its four per-horizon charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md
@.planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md
@forward_returns.py
@main.py
@exploration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port forward_returns.py data layer (load, pure-polars lookup, build_forward_returns)</name>
  <files>forward_returns.py</files>
  <read_first>
    - forward_returns.py — current pandas implementation (the methodology to preserve). Read `ensure_utc`/`normalize_nq_columns`/`timestamp_ns_utc`/`find_sorted_pos` (the lookup being replaced), `build_forward_returns` (the per-event/per-horizon loop + positional window), and the three pure-math helpers `candle_direction`/`direction_normalized_return`/`direction_normalized_profile`.
    - main.py — `load_data()` (lines ~85-119): the polars tz/precision normalization template (rename datetime_utc to DateTime_UTC, branch on time_zone, sort) and the `nq.slice(start_idx+1, length)` positional-window precedent (~149-152).
    - .planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md — "Shared Patterns -> Pure-polars exact-match lookup", forward_returns section 1 (load/normalize), section 2 (positional window), section 4 (pure-math helpers), and the us->ns / fill_null!=fillna pitfall notes.
    - .planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md — D-01, D-03, D-04, D-05, D-07 and "Claude's Discretion".
  </read_first>
  <action>
    Replace the pandas import with `import polars as pl`; KEEP `import numpy as np` (still needed by the helpers for `np.nan` and by the charts in Task 2) and the `matplotlib.use("Agg")`/pyplot block. Remove `import pandas as pd`.

    Delete `ensure_utc`, `normalize_nq_columns`, `timestamp_ns_utc`, and `find_sorted_pos`. Replace the load/normalize step with the polars approach from main.py `load_data()`: read both inputs via `pl.read_parquet(path, use_pyarrow=False)`; if `nq` has `datetime_utc` and not `DateTime_UTC`, `rename({"datetime_utc": "DateTime_UTC"})` (the on-disk column IS `datetime_utc`); normalize `DateTime_UTC` (naive -> `dt.replace_time_zone("UTC")`, aware -> `dt.convert_time_zone("UTC")`) and likewise normalize the events `datetime_utc`. CRITICAL (D-05): the nq column is `Datetime(us, UTC)` and events is `Datetime(ns, UTC)` — force a common unit with `pl.col("DateTime_UTC").dt.cast_time_unit("ns")` on the nq side (and ensure the event key is ns/UTC) BEFORE any equality/join key, or every `==` fails and all 5067 events silently drop. Then `nq = nq.sort("DateTime_UTC")`.

    Implement the pure-polars exact-match lookup (D-01/D-03, the SAME mechanism injection.py uses in plan 02): build `nq_idx = nq.with_row_index("idx")` once after sort. Resolve row indices by an inner-join (exact-match -> misses drop, reproducing `find_sorted_pos` -> `None` -> `continue`, D-04; do NOT use join_asof). Build the set of wanted timestamps (each event `datetime_utc`, plus `event_datetime + pl.duration(minutes=h)` for every horizon `h`), inner-join against `nq_idx.select(["DateTime_UTC", "idx"])` on the timestamp, and collapse the matched result into a python `dict` timestamp->idx (via `to_list()` zip). A per-event O(n) `.filter(== ts)` is forbidden here — at 5.3M rows it is the slow-scan anti-pattern; the join resolves all lookups in one pass.

    Rewrite `build_forward_returns` to keep the existing per-event/per-horizon loop shape (`for event in events.iter_rows(named=True)`), but resolve `release_idx = ts_to_idx.get(event_time)` (skip with `continue` on miss, D-04) and `future_idx = ts_to_idx.get(future_time)` per horizon (skip on miss). Get scalar candle fields via `nq.row(release_idx, named=True)` / `nq.row(future_idx, named=True)` (Open/High/Low/Close). Preserve the positional window (D-07): `window = nq.slice(release_idx + 1, future_idx - release_idx)` covers `[release_idx+1, future_idx]` inclusive; guard `if window.is_empty(): continue`; `window_high = window.get_column("High").max()`, `window_low = window.get_column("Low").min()`. Carry `candle_direction`, `direction_normalized_return`, `direction_normalized_profile` over verbatim (they keep returning `np.nan` for flat — intentional, see D-09 in Task 2). Build the output frame with `pl.DataFrame(rows)` (dtype pinning via an explicit schema is optional discretion; output parity is not required).
  </action>
  <acceptance_criteria>
    - `grep -nE "import pandas|from pandas|np\.searchsorted|join_asof" forward_returns.py` returns NO matches (D-01, D-04; pandas and searchsorted fully removed from this file).
    - `import numpy as np` is still present (helpers/charts need it).
    - Loading the real data and calling `build_forward_returns(events, nq)` returns a polars DataFrame with height > 0 — proves D-05 normalization holds and events are not silently dropped (the us->ns cast was applied).
    - The lookup is inner-join / equality exact-match (no `join_asof`, no per-event `.filter(pl.col("DateTime_UTC") == ...)` scan over the full frame); `with_row_index` is used to obtain the row index for the window.
    - The window is built with `nq.slice(release_idx + 1, future_idx - release_idx)` (positional `[release_idx+1, future_idx]`, D-07), not a time filter.
  </acceptance_criteria>
  <verify>
    <automated>! grep -nE "import pandas|from pandas|np\.searchsorted|join_asof" forward_returns.py && grep -q "with_row_index" forward_returns.py && python3 -c "import polars as pl, forward_returns as fr; ev=pl.read_parquet('data/economic_events.parquet'); nq=pl.read_parquet('data/nq_1m.parquet'); df=fr.build_forward_returns(ev,nq); assert df.height>0, 'empty -> events dropped (D-05 broken)'; print('rows', df.height)"</automated>
  </verify>
  <done>forward_returns.py loads via polars, resolves candle lookups with a pure-polars exact-match inner-join (no pandas, no searchsorted), preserves exact-match-or-skip (D-04), ns/UTC precision (D-05), and the positional window (D-07); build_forward_returns returns a non-empty polars frame on the real data.</done>
</task>

<task type="auto">
  <name>Task 2: Port forward_returns.py summaries, charts, and run() to polars</name>
  <files>forward_returns.py</files>
  <read_first>
    - forward_returns.py — `summarize_returns`, `summarize_path_profiles` (the pandas groupby/agg + `.notna()` filter being ported), the four `plot_*` functions (pandas selection + Series into matplotlib), and `run()`/`write_outputs` (pandas read/csv/print).
    - exploration.py — `build_summary_table` (~75-96) and `compute_win_rates` (~43-52): the polars `group_by().agg(...)` + `pl.when/then` template; `plot_event_win_rates` (~112-119): the `.to_numpy()`/`.to_list()` matplotlib boundary; `run()` (~203-258): `pl.read_parquet`, `mkdir`, `write_csv`, printing frames.
    - .planning/phases/02-independent-pipelines-on-polars/02-PATTERNS.md — forward_returns section 3 (group_by/agg, D-08/D-09), section 5 (matplotlib boundary), section 6 (entry point), and the pandas->polars idiom-swap table.
    - .planning/phases/02-independent-pipelines-on-polars/02-CONTEXT.md — D-08, D-09.
  </read_first>
  <action>
    Port `summarize_returns` and `summarize_path_profiles` to polars `group_by(...).agg(...)` (D-08): `count = pl.len()`; mean/median via `pl.col(c).mean()` / `pl.col(c).median()`; `win_rate` and `continuation_rate` as `(pl.col(c) > 0).mean() * 100`; `p25`/`p75` via `pl.col(c).quantile(0.25)` / `.quantile(0.75)`. Group keys: `["horizon_minutes", "news_candle_direction"]` for the raw summary, `["horizon_minutes"]` for the normalized and path-profile summaries. polars `group_by` does not preserve order — add `.sort("horizon_minutes", ...)` so the printed table is stable (cosmetic per D-08).

    Preserve the flat-direction exclusion (D-09): the direction-normalized columns carry float `np.nan` (the helpers emit `np.nan`, not polars null), so filter with `pl.col("direction_normalized_return_pct").is_not_nan()` (and `direction_normalized_mfe_pct.is_not_nan()` for the path summary) — `is_not_null()` would NOT drop NaN and would corrupt the summary. Keep the exclusion exact.

    Port the four `plot_*` functions: replace `df[df["horizon_minutes"] == h]` with `df.filter(pl.col("horizon_minutes") == h)`; feed matplotlib via `.to_numpy()` / `.to_list()` at the boundary (boxplot inputs become a list of numpy arrays built by filtering per direction). In `plot_normalized_mae_mfe_scatter`, replace `for direction, subset in df.groupby(...)` with an explicit ordered `for d in ["up","down","flat"]: subset = df.filter(pl.col("news_candle_direction") == d)` loop (polars group_by is unordered). For the median axvline in `plot_direction_normalized`, compute the median from the polars Series (`.median()`) or `np.median` on the extracted array. Keep `np.random`/`np.full` jitter as-is.

    Port `run()`/`write_outputs`: `pl.read_parquet(path, use_pyarrow=False)`; `if df.is_empty(): raise ValueError("No forward return rows produced; check event/NQ timestamp alignment")`; `df.write_csv(output_dir / "forward_returns_by_event.csv")`; iterate horizons via `sorted(df.get_column("horizon_minutes").unique().to_list())`; print summaries with `print(df.round(4))` under `with pl.Config(tbl_rows=-1):` (cf. main.py). Keep the existing `run()`/`parse_args()` entry shape.
  </action>
  <acceptance_criteria>
    - `python3 forward_returns.py` exits 0.
    - `charts/forward_returns/forward_returns_by_event.csv` exists and the four per-horizon PNGs are written (raw_by_direction, direction_normalized, mae_mfe_by_direction, normalized_mae_mfe_scatter).
    - Summaries use polars `group_by(...).agg(...)` (grep `group_by` in forward_returns.py) with win_rate/continuation_rate as `(pl.col(...) > 0).mean() * 100`.
    - The direction-normalized exclusion uses `is_not_nan` (grep), NOT `is_not_null`, on the direction-normalized columns (D-09).
    - `forward_returns.py` has zero `import pandas`; CSV is written via `write_csv` (no `to_csv`).
  </acceptance_criteria>
  <verify>
    <automated>python3 forward_returns.py && test -f charts/forward_returns/forward_returns_by_event.csv && ls charts/forward_returns/*.png >/dev/null && ! grep -nE "import pandas|from pandas|\.to_csv\(" forward_returns.py && grep -q "is_not_nan" forward_returns.py && grep -q "group_by" forward_returns.py</automated>
  </verify>
  <done>forward_returns.py runs end-to-end on polars; summaries use group_by/agg with the is_not_nan exclusion (D-08/D-09); charts feed matplotlib via the .to_numpy()/.to_list() boundary; run() reads/writes via polars and the CSV + four per-horizon PNGs are produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/*.parquet -> forward_returns.py | Local, read-only, irreplaceable raw inputs cross into the analysis script. No network, no untrusted/user-supplied input, no package-manager installs in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure (silent data loss) | timestamp lookup in build_forward_returns | mitigate | Cast nq DateTime_UTC to ns/UTC before equality/join (D-05); acceptance asserts build_forward_returns height > 0 so a precision-mismatch drop is caught, not silently shipped. |
| T-02-02 | Tampering | data/nq_1m.parquet, data/economic_events.parquet | mitigate | Reads only via pl.read_parquet(use_pyarrow=False); never write back to data/ inputs; no to_parquet on raw inputs (irreplaceable, gitignored — MEMORY constraint). |
| T-02-03 | Tampering (methodology corruption) | candle lookup | mitigate | Inner-join/equality exact-match only; miss -> skip (D-04). join_asof / nearest-match is forbidden — snapping to a neighbor candle would silently corrupt the sweep methodology (the project's core asset). |

No npm/pip/cargo install tasks in this plan (polars 1.40.1 already installed) -> no supply-chain (T-..-SC) checkpoint required.
</threat_model>

<verification>
- `python3 forward_returns.py` exits 0 and writes charts/forward_returns/forward_returns_by_event.csv + four PNGs per horizon (ROADMAP criterion 1).
- `grep -nE "import pandas|from pandas|np\.searchsorted" forward_returns.py` returns no matches (ROADMAP criterion 2 / D-01).
- build_forward_returns on the real data returns height > 0 (D-05 precision guard holds; events not silently dropped).
- Lookup is exact-match inner-join with `with_row_index`; window via `nq.slice(release_idx+1, future_idx-release_idx)` (D-04, D-07).
- Summaries use group_by/agg; direction-normalized exclusion via `is_not_nan` (D-08, D-09).
</verification>

<success_criteria>
forward_returns.py runs entirely on polars with no pandas and no np.searchsorted, produces its CSV
and four per-horizon charts, resolves candle lookups via a pure-polars exact-match inner-join that
preserves D-04 (exact-match-or-skip), D-05 (ns/UTC precision), and D-07 (positional window), and
computes summaries via polars group_by/agg with the D-09 is_not_nan exclusion. MIGRATE-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-independent-pipelines-on-polars/02-01-SUMMARY.md` when done.
</output>
